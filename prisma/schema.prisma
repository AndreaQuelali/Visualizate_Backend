// This is your Prisma schema file.
// Database URL is configured in prisma.config.ts — not here.
// learn more: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Domain models will be added here as the project evolves.
// Run `npm run prisma:migrate -- --name init` after adding your first model.
